
let removeDir = require('./util/removeDir');

let copyDir = require('./util/copyDir');

let flatDir = require('./util/flatDir');

// removeDir('src')

// copyDir('src','dist')

// flatDir('src','file.text')

module.exports = {
    removeDir,
    copyDir,
    flatDir
}